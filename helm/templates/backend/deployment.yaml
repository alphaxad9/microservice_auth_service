apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
  labels:
    app: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
    app.kubernetes.io/component: auth-service
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | default "1.0.0" | quote }}
    app.kubernetes.io/part-of: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount | default 1 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
  template:
    metadata:
      labels:
        app: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
        app.kubernetes.io/component: auth-service
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: auth
          image: "{{ .Values.backend.auth.image.repository }}:{{ .Values.backend.auth.image.tag }}"
          imagePullPolicy: {{ .Values.backend.auth.image.pullPolicy | default "IfNotPresent" }}
          
          ports:
            - name: http
              containerPort: {{ .Values.backend.auth.port }}
              protocol: TCP
          
          command: ["uvicorn", "my_backend.asgi:application", "--host", "0.0.0.0", "--port", "{{ .Values.backend.auth.port }}"]
          
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          
          livenessProbe:
            httpGet:
              path: /health/
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /health/
              port: http
            failureThreshold: 30
            periodSeconds: 10
          
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          
          # ✅ Mount ALL secrets (no duplication, single source of truth)
          envFrom:
            - configMapRef:
                name: {{ include "helm.appname" (dict "ctx" $ "svc" "auth") }}
            - secretRef:
                name: {{ .Values.secrets.auth }}
            - secretRef:
                name: {{ .Values.secrets.postgres }}
            - secretRef:
                name: {{ .Values.secrets.rabbitmq }}
            - secretRef:
                name: {{ .Values.secrets.kafka }}
      
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}