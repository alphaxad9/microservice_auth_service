name: Build and Push Auth Service Image

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: auth-service-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: auth-service
  REGISTRY: ghcr.io

jobs:

  test:
    runs-on: ubuntu-latest
    
    env:
      # Django core
      DJANGO_SECRET_KEY: django-insecure-test-key-for-ci-only-not-for-prod
      DJANGO_DEBUG: 'True'
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      
      # Use SQLite for tests
      POSTGRES_ENGINE: django.db.backends.sqlite3
      POSTGRES_DB: ':memory:'
      POSTGRES_HOST: ''
      POSTGRES_PORT: ''
      POSTGRES_USER: ''
      POSTGRES_PASSWORD: ''
      
      # PEM keys for JWT (test keys) - using literal block scalar for safety
      SIGNING_KEY: |
        -----BEGIN PRIVATE KEY-----
        
        -----END PRIVATE KEY-----
      VERIFYING_KEY: |
        -----BEGIN PUBLIC KEY-----
        
        qwIDAQAB
        -----END PUBLIC KEY-----
      
      # Disable external services for tests
      RABBITMQ_HOST: ''
      KAFKA_BROKERS: ''
      REDIS_URL: ''

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install deps
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-django

      - name: Run tests
        run: |
          pytest --cov=src --cov-report=xml -v --tb=short --ds=my_backend.settings

      - name: Upload coverage
        if: github.event_name == 'push'
        uses: codecov/codecov-action@v4
        with:
          file: coverage.xml
          fail_ci_if_error: false

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write
      security-events: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=long
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy scan
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          format: sarif
          output: trivy.sarif
          severity: CRITICAL,HIGH
          github-pat: ${{ secrets.GITHUB_TOKEN }}

      - uses: github/codeql-action/upload-sarif@v4 
        if: success()
        with:
          sarif_file: trivy.sarif